# 剧本杀管理后台前端

基于 Vue 3 + Element Plus 的管理后台系统。

## 技术栈

- **Vue 3** - 渐进式JavaScript框架
- **Vite** - 下一代前端构建工具
- **Element Plus** - Vue 3 UI组件库
- **Vue Router** - 官方路由管理器
- **Pinia** - 状态管理
- **Axios** - HTTP客户端

## 功能模块

- 🏠 仪表盘 - 数据统计展示
- 🏪 门店管理 - 门店和房间管理
- 📖 剧本管理 - 剧本和分类管理
- 📅 预约管理 - 预约订单管理
- 👥 用户管理 - 用户信息管理

## 开发

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

## Docker部署

### 构建镜像

```bash
docker build -t murder-admin-frontend .
```

### 运行容器

```bash
docker run -d -p 80:80 --name murder-admin-frontend murder-admin-frontend
```

### 使用Docker Compose

```bash
docker-compose up -d admin-frontend
```

## Nginx配置

前端使用Nginx作为Web服务器，支持：

- 静态资源服务
- 前端路由支持（History模式）
- API反向代理
- Gzip压缩
- 静态资源缓存

## 默认账号

- 用户名：admin
- 密码：admin123

## 目录结构

```
murder-admin-frontend/
├── public/              # 静态资源
├── src/
│   ├── assets/         # 资源文件
│   ├── layout/         # 布局组件
│   ├── router/         # 路由配置
│   ├── utils/          # 工具函数
│   ├── views/          # 页面组件
│   │   ├── dashboard/  # 仪表盘
│   │   ├── login/      # 登录
│   │   ├── store/      # 门店管理
│   │   ├── script/     # 剧本管理
│   │   ├── reservation/# 预约管理
│   │   └── user/       # 用户管理
│   ├── App.vue         # 根组件
│   └── main.js         # 入口文件
├── Dockerfile          # Docker构建文件
├── nginx.conf          # Nginx配置
├── vite.config.js      # Vite配置
└── package.json        # 依赖配置
```

## API接口

所有API请求通过Nginx代理到网关服务：

- 网关地址：http://murder-gateway:9090
- API前缀：/api

## 注意事项

1. 确保后端服务已启动
2. 检查网关地址配置是否正确
3. 生产环境建议使用HTTPS
4. 定期更新依赖版本
